# SwiftPM build output
.build/

# Release artifacts (DMGs are uploaded to GitHub Releases, not tracked in git)
*.dmg

# Local-only notes (manual-test logs, scratch) — never committed
*.local
*.local.md

# Archived Tauri app (superseded by the native app). Kept locally for reference;
# intentionally never tracked or pushed — the native app at the repo root is the product now.
.tauri/

# Tooling / OS
.claude/
.DS_Store
*.log
logs

# Editor directories
.vscode/*
!.vscode/extensions.json
.idea
*.swp

# Sparkle EdDSA private key export — NEVER commit (see docs/security/sparkle-update-security.md)
mallow-sparkle-privatekey.txt
# Release staging for appcast generation
/appcast-staging/
